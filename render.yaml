services:
  - type: web
    name: claude-ocr
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn ocr_haiku_first:app --bind 0.0.0.0:$PORT
    healthCheckPath: /health
    envVars:
      - key: ANTHROPIC_API_KEY
        sync: false
